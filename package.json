{
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "client": "yarn workspace client",
    "server": "yarn workspace server"
  },
  "dependencies": {
    "maid": "^0.3.0"
  }
}
